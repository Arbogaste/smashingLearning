# List of wave 0 tests to create
- [x] Fetch account balance 
- [x] Fetch models list 
- [x] Fetch free models (if user confirms with Y or Yes, they are saved to a file called [timestamp]_free_models.json)
- [x] fetch a specific model in order to have all the information about it

# List of wave 1 tests to create
- Call a model with a simple prompt and get the response (extimate time, cost, tokens used, etc)
- (Sequencially?) Fetch real cost of a model (they are saved to a file called [timestamp]_[model_name]_real_cost.json)

- Call a model with a simple prompt and get the response in streaming mode
- Call a model for video generation and download correctly the response in the output folder

